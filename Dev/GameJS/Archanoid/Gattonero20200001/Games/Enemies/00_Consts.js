/*
	定数
*/

/// Enemy_Kind_e
//
var<int> Enemy_Kind_e_CIRCLE = @(AUTO);
var<int> Enemy_Kind_e_SQUARE = @(AUTO);

/*
	ブロック用 - ブロックの種類
*/
/// Enemy_Block_Kind_e
//
var<int> Enemy_Block_Kind_e_SOFT = @(AUTO);
var<int> Enemy_Block_Kind_e_NORM = @(AUTO);
var<int> Enemy_Block_Kind_e_HARD = @(AUTO);
