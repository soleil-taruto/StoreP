/*
	B-0001
*/

/*
	ê∂ê¨
*/
function Enemy_B0001_Create(x, y)
{
	var ret =
	{
		Each: @@_Each,
		Dead: @@_Dead,
		X: x,
		Y: y,
	};

	return ret;
}

function @@_Each(enemy)
{
	//
}

function @@_Dead(enemy)
{
	//
}
